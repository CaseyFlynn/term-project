// export function for listening and communicating on the socket
module.exports = function (socket) {
    //in the future we can use this to emit information to our socket,
    //currently we are only relieving data from the socket
};
